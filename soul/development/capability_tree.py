"""Capability tree — definitions and prerequisites for all cognitive capabilities.

The tree definition is IMMUTABLE at runtime. The entity cannot modify its own
prerequisite definitions. Capabilities activate during dream cycles when all
prerequisites are met. Never use stage numbers.

Build phase: 0.2 (Week 1-2)
"""
